# Credentials -- .env holds a live Supabase DATABASE_URL. Never commit.
.env

# Virtualenvs
.venv/
venv/

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Data is NOT committed -- every output under data/ is reproducible by
# running `python3 src/run_pipeline.py` against the raw download.
# Note: `data/*` (not `data/`) so the README below can be re-included --
# git cannot un-ignore a file whose parent directory is itself excluded.
data/*
!data/README.md

# Tableau extracts (regenerable via src/07_export_tableau.py)
*.hyper
*.twbr

# macOS
.DS_Store

# Note: reports/figures/*.png is deliberately NOT ignored -- those are small
# and are the case-study evidence, regenerated by src/08_figures.py.
